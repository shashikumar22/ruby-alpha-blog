<%=form_for [@article, @comment] do |f| %>
	<p>
			<%= f.label :name %>
			<%= f.text_field :name %>
		</p>
		<p>
			<%= f.label :description %></br>
			<%= f.text_area :description %>
		</p>
		<%= f.submit %>
<% end %>